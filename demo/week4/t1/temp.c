#include <stdio.h>

double solve(double x);

int main() {
  double x;
  scanf("%lf", &x);
  double ans = solve(x);
  printf("%f\n", ans);
  return 0;
}

double solve(double x) {
  ////////////////

  double mysolve(double x);

  return mysolve(x);
}

#include <math.h>

double mysolve(double x) {
  return sqrt(x);

  ////////////////
}
